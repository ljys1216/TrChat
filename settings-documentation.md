# TrChat 配置选项说明

本文档旨在说明 `settings.yml` 文件中一些重要配置选项的用途和效果。

---

### `Options.Debug`

-   **类型:** `布尔值 (true / false)`
-   **默认值:** `false`

**作用:**

此选项用于控制是否在服务器控制台输出详细的插件调试信息。当启用时，TrChat 会打印出详细的运行日志，包括事件触发、数据处理、错误排查等信息。这对于开发者或服务器管理员诊断问题非常有用。

**建议:**

对于普通用户，强烈建议将此选项保持为 `false`。开启后会产生大量日志信息，可能会影响服务器性能并淹没控制台中的重要信息。仅在需要排查插件故障时开启。

---

### `Color.MiniMessage`

-   **类型:** `布尔值 (true / false)`
-   **默认值:** `true`

**作用:**

该选项决定是否启用 [MiniMessage](https://docs.adventure.kyori.net/minimessage/format.html) 格式解析器。MiniMessage 是一种功能强大的文本格式化语法，允许玩家和管理员在聊天中使用更高级的样式。

**效果:**

-   **当设置为 `true` 时:**
    -   玩家可以在聊天消息中使用 MiniMessage 标签，例如：
        -   `<red>你好</red>` (红色文本)
        -   `<gradient:blue:purple>渐变色</gradient>` (渐变色文本)
        -   `<hover:show_text:'这是一个提示'>悬浮提示</hover>` (悬浮文本)
        -   `<click:run_command:/say hello>点击执行命令</click>` (可点击的命令)
    -   插件会正确解析这些标签并将其渲染为对应的颜色、渐变、悬浮提示等效果。

-   **当设置为 `false` 时:**
    -   所有 MiniMessage 标签 (如 `<red>`, `<gradient>`) 都将被视为普通文本，不会被解析为任何特殊格式。

---

### `Color.Legacy`

-   **类型:** `布尔值 (true / false)`
-   **默认值:** `true`

**作用:**

此选项用于控制是否兼容并解析传统的 Minecraft 颜色代码，即使用 `&` 符号（例如 `&c` 代表红色，`&l` 代表粗体）。

**效果:**

-   **当设置为 `true` 时:**
    -   插件会识别并转换所有 `&` 颜色代码。例如，`&c你好 &l世界` 会被正确显示为红色的 "你好" 和粗体的 "世界"。
    -   这为习惯使用旧版颜色代码的用户提供了良好的兼容性。

-   **当设置为 `false` 时:**
    -   `&` 字符将被视为一个普通的文本字符，其后的代码不会被解析为颜色或样式。例如，`&c你好` 将会原样显示在聊天中。